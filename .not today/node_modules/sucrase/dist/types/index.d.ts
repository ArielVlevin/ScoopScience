import CJSImportProcessor from "sucrase/dist/types/CJSImportProcessor";
import { type RawSourceMap } from "sucrase/dist/types/computeSourceMap";
import { HelperManager } from "sucrase/dist/types/HelperManager";
import NameManager from "sucrase/dist/types/NameManager";
import type { Options, SourceMapOptions, Transform } from "sucrase/dist/types/Options";
import type { Scope } from "sucrase/dist/types/parser/tokenizer/state";
import TokenProcessor from "sucrase/dist/types/TokenProcessor";
export interface TransformResult {
    code: string;
    sourceMap?: RawSourceMap;
}
export interface SucraseContext {
    tokenProcessor: TokenProcessor;
    scopes: Array<Scope>;
    nameManager: NameManager;
    importProcessor: CJSImportProcessor | null;
    helperManager: HelperManager;
}
export type { Options, SourceMapOptions, Transform };
export declare function getVersion(): string;
export declare function transform(code: string, options: Options): TransformResult;
/**
 * Return a string representation of the sucrase tokens, mostly useful for
 * diagnostic purposes.
 */
export declare function getFormattedTokens(code: string, options: Options): string;
